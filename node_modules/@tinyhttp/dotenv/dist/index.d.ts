/// <reference types="node" />
import { DotenvParseOptions, DotenvParseOutput, DotenvConfigOptions, DotenvConfigOutput } from '../src/structs.js';
/**
 * Parses a string or buffer in the .env file format into an object.
 *
 * @param src - contents to be parsed
 * @param options - additional options
 * @returns an object with keys and values based on `src`
 */
export declare function parse(src: string | Buffer, options?: DotenvParseOptions): DotenvParseOutput;
/**
 * Loads `.env` file contents into {@link https://nodejs.org/api/process.html#process_process_env | `process.env`}.
 * Example: 'KEY=value' becomes { parsed: { KEY: 'value' } }
 *
 * @param options - controls behavior
 * @returns an object with a `parsed` key if successful or `error` key if an error occurred
 *
 */
export declare function config(options?: Partial<DotenvConfigOptions>): DotenvConfigOutput;
//# sourceMappingURL=index.d.ts.map
import { Template, Liquid, Tag, Emitter, Hash, TagToken, TopLevelToken, Context } from 'liquidjs';
import { ParsedFileName } from 'liquidjs/dist/tags/render';
export default class extends Tag {
    args: Hash;
    templates: Template[];
    file?: ParsedFileName;
    constructor(token: TagToken, remainTokens: TopLevelToken[], liquid: Liquid);
    render(ctx: Context, emitter: Emitter): Generator<unknown, unknown, unknown>;
}

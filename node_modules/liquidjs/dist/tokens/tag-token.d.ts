import { DelimitedToken } from 'liquidjs/dist/tokens/delimited-token';
import { Tokenizer } from 'liquidjs/dist/parser';
import type { NormalizedFullOptions } from 'liquidjs/dist/liquid-options';
export declare class TagToken extends DelimitedToken {
    name: string;
    tokenizer: Tokenizer;
    constructor(input: string, begin: number, end: number, options: NormalizedFullOptions, file?: string);
    get args(): string;
}

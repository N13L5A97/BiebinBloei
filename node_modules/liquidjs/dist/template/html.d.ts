import { TemplateImpl, Template } from 'liquidjs/dist/template';
import { HTMLToken } from 'liquidjs/dist/tokens';
import { Context } from 'liquidjs/dist/context';
import { Emitter } from 'liquidjs/dist/emitters';
export declare class HTML extends TemplateImpl<HTMLToken> implements Template {
    private str;
    constructor(token: HTMLToken);
    render(ctx: Context, emitter: Emitter): IterableIterator<void>;
}

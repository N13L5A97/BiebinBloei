import { TemplateImpl } from 'liquidjs/dist/template/template-impl';
import type { Emitter } from 'liquidjs/dist/emitters/emitter';
import type { Tokenizer } from 'liquidjs/dist/parser';
import type { Context } from 'liquidjs/dist/context/context';
import type { TopLevelToken, TagToken } from 'liquidjs/dist/tokens';
import type { Template } from 'liquidjs/dist/template/template';
import type { Liquid } from 'liquidjs/dist/liquid';
export type TagRenderReturn = Generator<unknown, unknown, unknown> | Promise<unknown> | unknown;
export declare abstract class Tag extends TemplateImpl<TagToken> implements Template {
    name: string;
    liquid: Liquid;
    protected tokenizer: Tokenizer;
    constructor(token: TagToken, remainTokens: TopLevelToken[], liquid: Liquid);
    abstract render(ctx: Context, emitter: Emitter): TagRenderReturn;
}
export interface TagClass {
    new (token: TagToken, tokens: TopLevelToken[], liquid: Liquid): Tag;
}

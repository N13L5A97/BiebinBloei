import { ParseStream } from 'liquidjs/dist/parser/parse-stream';
import { TopLevelToken } from 'liquidjs/dist/tokens';
import { Template, Output, HTML } from 'liquidjs/dist/template';
import { LookupType } from 'liquidjs/dist/fs';
import type { Liquid } from 'liquidjs/dist/liquid';
export declare class Parser {
    parseFile: (file: string, sync?: boolean, type?: LookupType, currentFile?: string) => Generator<unknown, Template[], Template[] | string>;
    private liquid;
    private fs;
    private cache?;
    private loader;
    constructor(liquid: Liquid);
    parse(html: string, filepath?: string): Template[];
    parseTokens(tokens: TopLevelToken[]): Template[];
    parseToken(token: TopLevelToken, remainTokens: TopLevelToken[]): import("liquidjs/dist/template").Tag | Output | HTML;
    parseStream(tokens: TopLevelToken[]): ParseStream<TopLevelToken>;
    private _parseFileCached;
    private _parseFile;
}

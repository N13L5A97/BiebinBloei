import { Drop } from 'liquidjs/dist/drop/drop';
import { Comparable } from 'liquidjs/dist/drop/comparable';
export declare class NullDrop extends Drop implements Comparable {
    equals(value: any): boolean;
    gt(): boolean;
    geq(): boolean;
    lt(): boolean;
    leq(): boolean;
    valueOf(): null;
}
